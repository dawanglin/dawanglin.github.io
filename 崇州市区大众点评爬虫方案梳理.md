# 崇州市区大众点评商铺爬虫方案梳理（基于 `Sniper970119/dianping_spider` 思路）

> 目标：围绕“大众点评-崇州市区”实现一个可持续运行、可增量更新、可监控质量的商铺采集方案。

## 1. 合规与边界（先做）

1. **遵守目标站点服务条款与 robots 规则**，仅采集业务必要字段。
2. **控制采集频率**，避免对目标站造成异常压力。
3. **数据用途合规**：仅用于内部分析/研究，不进行违规传播。
4. 采集链路中涉及账号、Cookie、代理等信息时，统一放入密钥管理（`.env` + KMS）。

---

## 2. 结合原仓库可复用的能力模块

从公开资料看，`dianping_spider` 的核心经验集中在：

- 反爬对抗：请求节流、Cookie 池、代理切换。
- 字体反混淆：针对点评页面字体映射做解码。
- 分页与详情抓取：列表页 -> 详情页 -> 评论页的分层抓取。

建议按“能力迁移”方式落地，而不是一次性全量照搬：

1. 先实现**列表页稳定抓取**（店铺ID/名称/商圈/分类）。
2. 再接入**详情页字段**（评分、价格、地址、电话、营业时间等）。
3. 最后视需求接入**评论维度**（高风险、成本高，可后置）。

---

## 3. 崇州市区范围定义（关键）

要避免“全成都误抓”，需先定义范围策略：

### 3.1 行政范围白名单
- 省市区：`四川省-成都市-崇州市`。
- 地址文本命中关键词：`崇州`、`崇阳`、`羊马`、`街子`、`元通`、`怀远` 等（按业务再细化）。

### 3.2 地理围栏（推荐）
- 获取崇州市边界 Polygon（高德/天地图/自有GIS）。
- 店铺经纬度落点后，做 `point-in-polygon` 判定。
- 行政白名单 + 地理围栏双判定，降低误差。

### 3.3 商圈/频道入口
- 以“美食、丽人、酒店、亲子、休闲娱乐”等频道拆任务。
- 每个频道维护独立分页游标，便于断点续爬。

---

## 4. 数据模型设计（建议最小可用字段）

### 4.1 店铺主表 `shops`
- `shop_id`（唯一键）
- `name`
- `city`, `district`, `biz_area`
- `category_lv1`, `category_lv2`
- `rating`, `review_count`, `avg_price`
- `address`, `phone`, `hours`
- `lng`, `lat`
- `source_url`
- `first_seen_at`, `last_seen_at`

### 4.2 抓取任务表 `crawl_jobs`
- `job_id`, `channel`, `page_no`
- `status`（pending/running/success/failed）
- `retry_count`, `error_msg`
- `started_at`, `finished_at`

### 4.3 原始快照表 `raw_html_snapshots`（可选）
- 保存关键页面源码（抽样），便于排查字体映射变更。

---

## 5. 抓取流程（生产可执行版）

1. **任务生成器**：按频道 + 区域 + 分页生成任务。
2. **列表页抓取器**：提取店铺卡片基础字段与详情链接。
3. **详情页抓取器**：补全评分、价格、营业信息等。
4. **字段标准化**：地址清洗、价格数字化、评分范围校验。
5. **区域过滤**：地址关键词 + 经纬度围栏双重校验。
6. **入库去重**：`shop_id` Upsert；字段变化做审计日志。
7. **失败重试**：指数退避；超阈值入死信队列。
8. **质量监控**：每天输出抓取量、成功率、字段缺失率。

---

## 6. 反爬与稳定性策略

1. **请求频率**：全局QPS + 随机抖动（例如 0.8~2.5s）。
2. **代理池**：按失败率淘汰，按健康度加权分配。
3. **Cookie池**：按场景（列表/详情）隔离，失败自动切换。
4. **字体映射缓存**：按 CSS/字体 hash 做本地缓存，避免重复解析。
5. **熔断保护**：连续高失败率时自动降速或暂停任务。

---

## 7. 增量更新策略

### 日常增量（T+1）
- 仅抓取最近活跃频道分页前 N 页。
- 命中历史 `shop_id` 时只更新易变字段（评分/评论数/价格）。

### 周级全量（每周一次）
- 对崇州市区频道做深分页扫全。
- 用于发现新店、闭店、迁址等变化。

---

## 8. 验收指标（建议）

1. **覆盖率**：与人工抽样清单对比，覆盖率 ≥ 90%。
2. **准确率**：崇州市区归属准确率 ≥ 95%。
3. **稳定性**：7日平均成功率 ≥ 85%。
4. **时效性**：增量任务当日完成率 ≥ 95%。

---

## 9. 两周落地排期（MVP）

### 第1周
- Day 1-2：需求字段冻结 + 数据表建模。
- Day 3-4：列表页抓取 + 区域过滤初版。
- Day 5：详情页抓取与入库。

### 第2周
- Day 1-2：重试/限速/代理与Cookie策略接入。
- Day 3：质量报表与告警。
- Day 4：抽样验收与参数调优。
- Day 5：上线与运行手册。

---

## 10. 给你的实际执行建议（下一步）

1. 先只做“崇州市区 + 美食频道”单频道试跑。
2. 跑通后再扩品类，避免一开始复杂度过高。
3. 先保证“列表+详情”稳定，再考虑评论抓取。
4. 预留字体/结构变更快速修复机制（配置化 + 快照对比）。

